# Optional overrides for production. Copy to .env.production on the server (gitignored).
# Loaded after .env when APP_ENV=production

# DATABASE_URL=postgresql://user:pass@prod-host:5432/sri_demo
# ELASTICSEARCH_URL=https://your-prod-deployment.elastic-cloud.com:443
# ELASTICSEARCH_API_KEY=your_production_api_key_here
